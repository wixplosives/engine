import { Feature } from '@wixc3/engine-core';
import {{pascalCase featureName}} from '../../feature';

export default new Feature({
    id: '{{camelCase fixtureName}}',
    dependencies: [{{pascalCase featureName}}.asEntity],
    api: {}
});
