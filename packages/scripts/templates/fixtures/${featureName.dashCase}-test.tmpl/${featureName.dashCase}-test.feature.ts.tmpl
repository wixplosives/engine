import { Feature } from '@wixc3/engine-core';
import ${featureName.pascalCase} from '../../feature';

export default new Feature({
    id: '${featureName.camelCase}Test',
    dependencies: [${featureName.pascalCase}.asEntity],
    api: {}
});
