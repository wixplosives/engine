// import ${featureName.pascalCase} from './${featureName.dashCase}.feature';
// import { Environment } from '@wixc3/engine-core';
// const mainEnv = new Environment('main', 'window', 'single');

// ${featureName.pascalCase}.setup(mainEnv, ({ run, ${featureName.camelCase}Options }, {}) => {
//     // console.log(${featureName.camelCase}Options);
// 
//     run(() => {
//         // run
//     });
// });
